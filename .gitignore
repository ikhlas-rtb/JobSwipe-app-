# Node / JavaScript
# node_modules (global) et logs
node_modules/
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# Environnements (ne pas commiter les secrets)
.env
.env.*.local
.env.local
.env.development.local
.env.test.local
.env.production.local

# Build / artefacts
dist/
build/
out/
.cache/
.next/
.vite/
public/build/

# Coverage
coverage/
coverage-final.json
nyc_output/

# Logs & processus
logs
*.log
pids
*.pid
*.seed
*.pid.lock

# Docker
# fichiers d'environnement locaux pour docker-compose, images temporaires
docker-compose.override.yml
docker-compose.*.yml
*.dockerignore

# Backend / Frontend per-directory ignores (si des node_modules locaux existent)
backend/node_modules/
backend/dist/
backend/build/
backend/.env

frontend/node_modules/
frontend/dist/
frontend/build/
frontend/.env

# Database dumps / DB files (ne pas versionner les dumps/binaires)
database/*.sqlite
database/*.db
database/*.sql
database/*.sql.gz
database/*.dump

# Editor / IDE
.vscode/
.idea/
*.sublime-workspace
*.sublime-project

# OS files
.DS_Store
Thumbs.db
desktop.ini

# Archives / packaging / executables
*.tgz
*.zip
*.tar.gz
*.exe
*.dll

# Misc
*.local
*.log.*
*.tmp
*.temp

# Keep lockfiles (package-lock.json / yarn.lock) under version control,
# do not ignore them here (important for reproducible installs).